apiVersion: v1
kind: ConfigMap
metadata:
  name: docvault-backend-config
  namespace: docvault
data:
  # Application configuration
  UPLOAD_DIR: "/app/uploads"
  DB_DIR: "/app/data"
  JSON_DB_PATH: "/app/data/json_db"
  
  # Logging
  LOG_LEVEL: "INFO"
  
  # Performance
  WORKERS: "4"

